#include "main.h"

/**
 * mul - multiply 2 numbers
 * @a: the first number
 * @b: the second number
 * Return: the result of the multiplication
 */

int mul(int a, int b)
{
	int r;

	r = a * b;

	return (r);
}
